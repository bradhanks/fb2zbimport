var fresh2zip = require('./fresh2zip.js');

if (false) {
  fresh2zip.fresh2zip();
} else {
  fresh2zip.fresh2zip(
    '',                     // email
    '',                                                     // password
    '',                             // freshbooks token
    '' // freshbooks api subdomain
    );
}
